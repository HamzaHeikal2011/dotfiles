#!/bin/bash

# omarchy:summary=Finish the installation of Omarchy with items that can only be done after logging in.
# omarchy:requires-sudo=true

set -e

FIRST_RUN_MODE=~/.local/state/omarchy/first-run.mode

if [[ -f $FIRST_RUN_MODE ]]; then
  rm -f "$FIRST_RUN_MODE"

  bash "$DOTFILES_PATH/install/first-run/battery-monitor.sh"
  bash "$DOTFILES_PATH/install/first-run/recover-internal-monitor.sh"
  bash "$DOTFILES_PATH/install/first-run/cleanup-reboot-sudoers.sh"
  bash "$DOTFILES_PATH/install/first-run/firewall.sh"
  bash "$DOTFILES_PATH/install/first-run/dns-resolver.sh"
  bash "$DOTFILES_PATH/install/first-run/gnome-theme.sh"
  bash "$DOTFILES_PATH/install/first-run/elephant.sh"
  sudo rm -f /etc/sudoers.d/first-run

  bash "$DOTFILES_PATH/install/first-run/welcome.sh"
  bash "$DOTFILES_PATH/install/first-run/wifi.sh"
fi
