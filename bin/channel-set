#!/bin/bash

# omarchy:summary=Set the Omarchy channel, which dictates what git branch and package repository is used.
# omarchy:args=<stable|rc|edge|dev>
# omarchy:requires-sudo=true

if (($# == 0)); then
  echo "Usage: channel-set [stable|rc|edge|dev]"
  exit 1
else
  channel="$1"
fi

case "$channel" in
"stable") omarchy-branch-set "master" && refresh-pacman "stable" ;;
"rc") omarchy-branch-set "rc" && refresh-pacman "rc" ;;
"edge") omarchy-branch-set "master" && refresh-pacman "edge" ;;
"dev") omarchy-branch-set "dev" && refresh-pacman "edge" ;;
*) echo "Unknown channel: $channel"; exit 1; ;;
esac

update -y
