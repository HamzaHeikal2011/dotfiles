#!/bin/bash

# omarchy:summary=Launch a floating terminal with the Omarchy presentation wrapper
# omarchy:args=<command>

cmd="$*"
exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.terminal -e bash -c "~/.dotfiles/bin/show-logo; $cmd; if (( \$? != 130 )); then ~/.dotfiles/bin/show-done; fi"
